# AGENTS

## Project Context
- The project specification is located at `docs/specification.md`.

## Frontend Requirements
- The frontend should use DaisyUI.
- Reference: https://daisyui.com/llms.txt
- Use DaisyUI components wherever possible instead of building custom UI primitives from scratch.

## Code Quality Requirements
- Keep the code modular.
- Organize code into clear, reusable components/modules with single responsibilities.
